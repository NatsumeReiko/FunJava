<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="amy.com.funjava">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- <activity -->
        <!-- android:name=".SingleInstanceActivity001" -->
        <!-- android:label="SingleInstanceActivity001"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="android.intent.action.MAIN" /> -->
        <!-- <category android:name="FUN_JAVA" /> -->
        <!-- </intent-filter> -->


        <!-- </activity> -->

        <activity
            android:name=".ObjectActivity"
            android:label="ObjectActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="FUN_JAVA" />
            </intent-filter>

        </activity>

        <activity
            android:name=".SetClassLabActivity"
            android:label="SetClassLabActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="FUN_JAVA" />
            </intent-filter>

        </activity>

        <activity
            android:name=".MultiThreadActivity001"
            android:label="MultiThreadActivity001">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="FUN_JAVA" />
            </intent-filter>

        </activity>

        <activity
            android:name=".JavaReflection"
            android:label="JavaReflection">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="FUN_JAVA" />
            </intent-filter>

        </activity>

        <activity
            android:name=".VolatileLab"
            android:label="Volatile">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="FUN_JAVA" />
            </intent-filter>
        </activity>

        <activity
            android:name=".VolatileLab_object"
            android:label="VolatileLab_object">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="FUN_JAVA" />
            </intent-filter>
        </activity>

    </application>

</manifest>
